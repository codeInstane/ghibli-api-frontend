import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '4S-api-frontend',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
